# Smart Apply — Python Dependencies
# Install: py -m pip install -r requirements.txt

# Core (already installed)
requests>=2.28.0
feedparser>=6.0.0

# Phase 2: Universal scraper
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Browser automation (choose one or both)
# Playwright (recommended - faster, more reliable)
playwright>=1.30.0

# Selenium (fallback)
# selenium>=4.8.0
# webdriver-manager>=3.8.0
